# Lab Result Entry (Structured Templates) — 2027 Modernization Plan

**Document type**: Implementation plan, following the same conventions as `reports/order-creation-v2-modernization-plan.md` (extract-don't-rewrite, phased delivery, explicit scope table, status column). Built on findings in [lab-result-templates-current-state-audit.md](lab-result-templates-current-state-audit.md) (what exists) and [lab-result-templates-problems-audit.md](lab-result-templates-problems-audit.md) (what's wrong).

**Why this exists now**: a Tanzania-standard structured Stool Analysis result form was requested to replace free-text entry. The audit found the structured system (`ResultTemplate` / `StructuredLabResultForm`) already exists and Stool Analysis is already fully modeled — what's needed is not a rebuild, it's fixing a live bug, closing one real field gap (Remarks/Impression), and hardening the data layer so this pattern scales cleanly to the rest of the Tanzanian standard test panel list. This plan is scoped as fixes and hardening on top of working code, not a rewrite.

## 1. Design decision: keep the existing `ResultTemplate` architecture

The `sections: [{ label, fields: [{code, label, type, options?}] }]` model, driven entirely from catalog `metadata.resultTemplate` JSON and rendered by one generic `StructuredLabResultForm.vue`, is the right shape for a modern LIS: adding a new test type's structured form is a data change (new catalog JSON), not a new Vue component. **Do not build a Stool-specific component** — that would regress the one part of this system that's already correctly generalized, and would need to be repeated for every future test type (Urinalysis, Malaria, HIV, Blood Group, Sputum, Widal already share this engine).

## 2. Phased plan

| Phase | Content | Depends on | Status |
|---|---|---|---|
| **1 — Fix the live crash bug** | Replace `<SelectItem value="">—</SelectItem>` at `LaboratoryStatusUpdateDialog.vue:365-367` with the same pattern used in the three prior fix commits (`d5b4ea5`, `449d5bd`, `ca29973`): bind the row's `flag` to `undefined` instead of `''`, and render the "clear" option via `SelectTrigger`'s placeholder rather than a real `SelectItem`. Reachable today from Stool Microscopy (`LAB-STOOL-MICRO-001`) and every other panel-style test. | — | **Done** (`3c2dddf`) — verified in-browser against a live in_progress Stool Microscopy order: dropdown opens with no crash, only the 4 real options render, selection works. |
| **2 — Add Remarks + Impression to templated results** | (a) Extended `ResultTemplateField.type` in `resultTemplate.ts` with a `'textarea'` variant. (b) Added `remarks`/`impression` as fields always rendered by `StructuredLabResultForm.vue` outside the section loop (a "Remarks & Impression" card at the end of the form), independent of template JSON — every current and future templated test gets it automatically with zero catalog-data changes. (c) Added the `'textarea'` case to the field-type chain for forward compatibility with any future template-declared textarea field. `buildResultSummaryFromTemplate`/`buildResultParametersFromTemplate` updated to include both. | 1 (share a PR/branch, not a hard dependency) | **Done** (`abde062`) — verified end-to-end against a temporary templated catalog item + order: filled Remarks/Impression, submitted, confirmed both `resultSummary` (sectioned text) and `resultParameters` (flat array) persisted correctly. Test data cleaned up after. |
| **3 — Consolidate template source of truth** | Deploys only run `migrate`, not `db:seed` (confirmed in `docker/start.sh`), so both mechanisms genuinely have to keep existing — but they no longer duplicate data. Added `LaboratoryClinicalCatalogSeeder::resultTemplates()`, a code-keyed accessor derived from the existing `LAB_TEST_BLUEPRINTS` const, and the migration now consumes it instead of embedding its own copy of the JSON. | — | **Done** (`ae1bc18`) — verified against the dev DB: ran the migration up (each code got its template), rolled back (templates removed), re-ran (idempotent, "Nothing to migrate"). |
| **4 — Fix catalog data bugs** | (a) Removed the duplicate blueprint entries for `LAB-BGRH-001`, `LAB-MPS-001`, `LAB-HIV-001` in the seeder — kept only the template-bearing definition, deleted the earlier no-template block for each. (b) Malaria substring collision — resolved as a side effect of Phase 3 (migration now matches by exact `code`). | 3 | **Done** (`8c59028`) — verified by re-running the seeder against the dev DB: 22 items seeded per facility (was 25 with dupes), each of the three codes resolves to exactly one row with its template intact. |
| **5 — Preserve section grouping through persistence and display** | Add a `section: string` field to `LabResultParameter` (`useLaboratoryOrders.ts:23-30`) and populate it in `buildResultParametersFromTemplate()` (`resultTemplate.ts:42-63`) from the section label each field belongs to. Update `LaboratoryOrderDetailSheet.vue:122-187` to group rows by `section` (e.g. a sub-header per group, matching the entry form's card-per-section layout) instead of one flat table — so a signed Stool Analysis report visually reproduces Macroscopic / Microscopic / Ova & Parasites / Occult Blood / Additional Tests, matching the brief's sample report format. Backward-compatible: `section` is optional/nullable, existing non-templated `resultParameters` (CBC, U&E, LFT, etc.) render exactly as today with no group header. | 2 | Not started |
| **6 — Resolve Stool Analysis vs. Stool Microscopy overlap** | Product decision, not a pure engineering one — recommend retiring `LAB-STOOL-MICRO-001` as a separately orderable item (its plain-panel fields are a strict subset of what `LAB-STOOL-001`'s template already captures) and pointing any existing references/history at the structured item, OR keeping both but relabeling clearly (e.g. "Stool Microscopy (quick panel)" vs. "Stool Analysis (full report)") if there's a real ordering-speed reason to keep a lighter option. Either way, fixing §1's crash bug on `LAB-STOOL-MICRO-001` (Phase 1) is not conditional on this decision — do that regardless. | 1 | Not started (needs a product decision first) |
| **7 — Test coverage** | Add `StructuredLabResultForm.spec.ts` (field-type rendering, multiselect toggle behavior, textarea from Phase 2) and `resultTemplate.spec.ts` (`buildResultSummaryFromTemplate`/`buildResultParametersFromTemplate`, including the new `section` field from Phase 5) using the Stool Analysis template as the primary fixture, since it exercises every field type (`select`, `not-done`, `text`, `number`, `multiselect`, `positive-negative`, and the new `textarea`). | 2, 5 | Not started |
| **8 — Fast-follow: expand template coverage** | Once Phases 1-7 land, the same pattern is proven and cheap to extend to any Tanzania-standard panel currently on the flat/plain fallback (e.g. Urine Pregnancy Test, HBsAg, VDRL/RPR are simple positive/negative tests that would benefit from the `positive-negative` field type instead of free text). Not a blocker for anything — purely additive catalog data. | 1-7 | Not started |

## 3. Non-goals

- Rebuilding `StructuredLabResultForm.vue` or the `ResultTemplate` type system from scratch — it's architecturally sound; the problems are data-duplication and one missing field type, not the design.
- A Stool-Analysis-specific Vue component — would break the one-engine-many-templates pattern already established for 7 test types.
- PDF/print-report generation, analytics/decision-support on structured results, or any consumer of `resultParameters` beyond the detail sheet — out of scope until the data itself is trustworthy (Phases 1-5).
- Touching the encounter-workflow result-entry path if one exists separately — this plan covers `laboratory-orders/IndexV2.vue`'s `LaboratoryStatusUpdateDialog.vue` only; confirm before Phase 1 whether encounter-context result entry shares this same dialog (it appears to, per the current-state audit, since there's only one `LaboratoryStatusUpdateDialog.vue` in the codebase) or has a separate surface that would also need the Phase 1/2 fixes.

## 4. Verification (per phase)

- Phase 1: manually complete a Stool Microscopy order through the panel-flag dropdown; confirm no console error, confirm the "clear" state still works.
- Phase 2: complete a Stool Analysis order end-to-end, confirm Remarks/Impression are saved and appear in `resultSummary`/`resultParameters`, confirm the detail sheet shows them.
- Phase 3/4: run the seeder fresh against an empty catalog table and confirm the resulting `resultTemplate` JSON for all 7 templated tests matches exactly one definition (no drift), and that `LAB-MRDT-001` has no `resultTemplate` while `LAB-MPS-001` does.
- Phase 5: complete a Stool Analysis order, open its detail sheet, confirm section headers (Macroscopic Examination, Microscopic Examination, Ova and Parasites, Occult Blood, Additional Tests, Remarks & Impression) render in the same order as the entry form.
- Phase 7: `vitest run` green on the new specs; existing suite unaffected.
- Full flow, throwaway-data pattern: order a Stool Analysis test end-to-end (create → collect → process → complete with all sections filled, including a multiselect ova finding and Remarks/Impression) → verify the signed result displays correctly in `LaboratoryOrderDetailSheet.vue` with grouped sections and matches the sample report format in the original brief.
